{
  "name": "zerollup",
  "version": "1.0.0",
  "description": "Zero rollup plugins and toolset",
  "private": true,
  "config": {
    "pkg": "*",
    "env": "production",
    "rollup": "../../rollup.config.js"
  },
  "scripts": {
    "clean": "rm -rf packages/*/.rpt2_cache packages/*/dist",
    "clean.all": "lerna clean --yes && rm -rf packages/*/package-lock.json node_modules packages/*/.rpt2_cache packages/*/dist docs/*",
    "build": "NODE_ENV=$npm_package_config_env lerna exec --scope @$npm_package_name/$npm_package_config_pkg -- rollup -c $npm_package_config_rollup",
    "dev": "NODE_ENV=development lerna exec --scope @$npm_package_name/$npm_package_config_pkg -- rollup -c $npm_package_config_rollup -w",
    "postinstall": "lerna bootstrap --hoist",
    "publish": "lerna publish",
    "dev.helpers": "npm run dev --zerollup:pkg=helpers",
    "dev.preset-ts": "npm run dev --zerollup:pkg=preset-ts",
    "dev.injector": "npm run dev --zerollup:pkg=injector",
    "dev.plugin-assets": "npm run dev --zerollup:pkg=plugin-assets",
    "dev.plugin-template": "npm run dev --zerollup:pkg=plugin-template"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^9.6.1",
    "lerna": "^2.9.1",
    "rollup": "^0.57.1",
    "typescript": "^2.8.1"
  },
  "dependencies": {}
}
